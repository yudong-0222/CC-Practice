#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n,k,a[110]={0,1}; 
    cin >> n;
    for (int i =2;i<=n;i++) {
        cin >> k;
        a[k+2]=i;
    }
    for (int i=1;i<=n;i++) {
        cout << a[i] << " ";
    }
}