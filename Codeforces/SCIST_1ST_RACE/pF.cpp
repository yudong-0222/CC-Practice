#include <bits/stdc++.h>
using namespace std;
#define fastio ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define int long long

int a[4][10] = {
  {1,0},
  {0,-1},
  {-1,0},
  {0,1}
};

signed main(void) {
	fastio;

  int n,m;
  cin >> n >> m;
  

  for(int i = 1; i <= m; ++i) {
    
  }

  if(m == 0) cout << n * n << "\n";

}